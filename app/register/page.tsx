export const metadata = { title: 'Register | BigDawg (Placeholder)' };

export default function RegisterPage() {
  return (
    <div style={{padding:'2.5rem 0', maxWidth:420}}>
      <h1 style={{fontSize:'1.75rem', marginBottom:'1rem'}}>Create Account</h1>
      <p className="muted" style={{marginBottom:'1.5rem'}}>Replace with registration form fields (email, password, username, marketing opt-in).</p>
      <div className="placeholder-box">Register Form Placeholder</div>
    </div>
  );
}
